---
title: "Create Tenant"
description: "Creating a tenant (space) where you can store your memories."
openapi: "POST /tenants/create"
---

If you pass `tenant_id` as a query parameter, that ID will be used. If not provided, the service generates a new `tenant_id` and returns it.

<Note>
  **After creating a tenant**: Poll **GET /tenants/infra/status?tenant_id=...** to verify provisioning is complete before starting ingestion. See [Monitor & infra status](/api-reference/endpoint/monitor-tenant#check-tenant-infra-status) for details.
</Note>

### Examples

<Tabs>
  <Tab title="API Request">
    ```bash expandable
    curl -X 'POST' \
    'https://api.hydradb.com/tenants/create' \
    -H 'accept: application/json' \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer <token>' \
    -d '{
    "tenant_id": "string"
    }'
    ```
  </Tab>
  <Tab title="Python (Sync)">
    ```python expandable
    # Async usage is similar, just use async_client and await
    tenant_response = client.tenant.create(
        tenant_id="tenant_1234",
        tenant_metadata_schema=[
            {
                "name": "department",
                "data_type": "string",
                "searchable": True,
                "filterable": True,
            },
            {
                "name": "compliance_framework",
                "data_type": "string",
                "searchable": True,
            },
            {
                "name": "data_classification",
                "data_type": "string",
                "filterable": True,
            },
        ]
    )
    ```
  </Tab>
</Tabs>

<Note>
  **Important**: When a tenant is created, HydraDB automatically creates a **DEFAULT sub-tenant** that acts as a global space for that tenant. Any documents ingested or API calls made with the `tenant_id` but without specifying a `sub_tenant_id` will automatically reference this default sub-tenant.
</Note>

## Creating Additional Sub-Tenants

**No Separate API Required**: HydraDB doesn't require a separate API call to create additional sub-tenants. Sub-tenants are created automatically when you first use a new `sub_tenant_id` that doesn't exist yet.

### How Sub-Tenant Creation Works

1. **Automatic Creation**: When you upload content with a `sub_tenant_id` that doesn't exist, HydraDB automatically creates that sub-tenant
2. **First Upload Triggers Creation**: The first API call with a new `sub_tenant_id` will create the sub-tenant and then process your content
3. **Immediate Availability**: Once created, the sub-tenant is immediately available for all subsequent operations

### Sub-Tenant ID Best Practices

**For B2C Applications (Individual Users):**

```javascript
// Use user IDs as sub-tenant IDs for personal workspaces
sub_tenant_id: "user_12345"
sub_tenant_id: "user_67890"
```

**For B2B Applications (Departments/Teams):**

```javascript
// Use descriptive department or team names
sub_tenant_id: "engineering"
sub_tenant_id: "sales"
sub_tenant_id: "hr"
sub_tenant_id: "marketing"
```

**For Project-Based Organizations:**

```javascript
// Use project identifiers
sub_tenant_id: "project_alpha"
sub_tenant_id: "client_acme_corp"
```

<Info>
  **Naming Convention**: Use consistent prefixes like `user_`, `dept_`, or `project_` to make the purpose of each sub-tenant clear. Avoid special characters and keep names descriptive but concise.
</Info>

<Warning>
  **Important**: Tenant metadata keys are **immutable** once set during tenant creation. Choose your keys carefully as they cannot be modified later. Document metadata remains fully flexible and can be set per document during upload.
</Warning>

## Reserved Keywords

The following keywords are **reserved** and cannot be used as keys in `tenant_metadata_schema`:

- `id`
- `source_title`
- `source_url`
- `source_type`
- `source_collection`
- `source_owner`
- `source_collaborator`
- `source_upload_time`
- `source_last_updated_time`
- `chunk_id`
- `chunk_uuid`
- `chunk_content`
- `document_metadata`
- `base_metadata`
- `layout`
- `description`

These keywords are used internally by HydraDB for document processing and search functionality. Using any of these reserved keywords in `tenant_metadata_schema` will result in an error during tenant creation.

## Metadata Schema Structure

Each schema object in the `tenant_metadata_schema` array should specify:

- **name**: The metadata field name (immutable once set)
- **data_type**: The field's data type (see supported types below)
- **searchable**: Whether this field can be searched semantically
- **filterable**: Whether this field can be used for exact filtering

## Supported Metadata Types

### Primitive Types

| Type        | Description       | Example Values                              | Use Cases                                  |
| ----------- | ----------------- | ------------------------------------------- | ------------------------------------------ |
| `string`    | Text data         | `"Engineering"`, `"SOC2"`, `"confidential"` | Categorical data, identifiers, labels      |
| `integer`   | Whole number      | `5`, `100`, `2024`                          | Counts, versions, priorities               |
| `float`     | Decimal number    | `0.95`, `3.14`, `99.9`                      | Scores, percentages, measurements          |
| `boolean`   | True/false        | `true`, `false`                             | Binary flags, status indicators            |

### Complex Types

| Type      | Description     | Example Values                      | Use Cases                                |
| --------- | --------------- | ----------------------------------- | ---------------------------------------- |
| `object`  | Structured data | `{"city": "SF", "country": "USA"}`  | Hierarchical relationships, nested data  |
| `array`   | Multiple values | `["security", "api", "compliance"]` | Multi-value attributes, tags, categories |

## Searchable vs Filterable

**`searchable: true`** — indexes the field for semantic and keyword search. Use when you want users to find documents by asking questions about this field.

```json
{
  "name": "department",
  "data_type": "string",
  "searchable": true
}
```

**`filterable: true`** — enables exact-match filtering (e.g. `department = "Engineering"`). Use for precise narrowing before semantic ranking.

```json
{
  "name": "data_classification",
  "data_type": "string",
  "filterable": true
}
```

**Combined — filterable and searchable:**

```json
{
  "name": "compliance_framework",
  "data_type": "string",
  "searchable": true,
  "filterable": true
}
```

## Comprehensive Examples

**Enterprise Setup - Multi-Department Organization**

```json
[
  { "name": "department",           "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "compliance_framework", "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "data_classification",  "data_type": "string",  "filterable": true },
  { "name": "business_unit",        "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "retention_period",     "data_type": "integer", "filterable": true },
  { "name": "is_confidential",      "data_type": "boolean", "filterable": true }
]
```

**Legal Firm Setup - Client and Case Management**

```json
[
  { "name": "practice_area",        "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "client_id",            "data_type": "string",  "filterable": true },
  { "name": "confidentiality_level","data_type": "string",  "filterable": true },
  { "name": "jurisdiction",         "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "case_priority",        "data_type": "integer", "filterable": true },
  { "name": "requires_approval",    "data_type": "boolean", "filterable": true }
]
```

**Engineering Team Setup - Project and Component Management**

```json
[
  { "name": "product_line", "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "team",         "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "component",    "data_type": "string",  "searchable": true, "filterable": true },
  { "name": "priority",     "data_type": "integer", "filterable": true },
  { "name": "is_critical",  "data_type": "boolean", "filterable": true },
  { "name": "tags",         "data_type": "array",   "filterable": true }
]
```

**Simple Setup - Basic Organization**

```json
[
  { "name": "organization", "data_type": "string", "searchable": true, "filterable": true },
  { "name": "environment",  "data_type": "string", "filterable": true }
]
```

## Metadata Best Practices

**1. Naming Conventions**

- **Use snake_case**: `business_unit` instead of `businessUnit`
- **Be descriptive**: `data_classification_level` instead of `classification`
- **Use consistent prefixes**: `compliance_*`, `security_*`, `business_*`

**2. Type Selection Guidelines**

- **`string`**: Text, labels, and any field that needs semantic or keyword search
- **`integer`**: Counts, versions, priorities
- **`float`**: Scores, percentages, measurements
- **`boolean`**: Binary flags and status indicators
- **`object`**: Structured, hierarchical data
- **`array`**: Multi-value fields like tags and categories

**3. Searchable vs Filterable Strategy**

- **`searchable: true`**: Fields users will ask natural-language questions about
- **`filterable: true`**: Fields used for precise exact-match filtering
- **Combine both**: For fields that need to support both free-text search and exact filtering
- Non-string types (`integer`, `boolean`) support scalar filtering without any flags needed

**4. Performance Considerations**

- **Limit total fields**: Keep metadata schema focused and purposeful
- **Use appropriate types**: Choose the most specific type for your data
- **Consider query patterns**: Design for your most common use cases
- **Plan for growth**: Design schema to accommodate future needs

### Query Examples

With the metadata schema defined above, users can perform sophisticated queries:

#### Semantic Search (Dense Embedding Fields)

```
"Find all engineering documents about security compliance"
```

- Searches through `department` (`searchable: true`)
- Searches through `compliance_framework` (`searchable: true`)
- Searches through document content and titles

#### Precise Filtering (Match / Scalar Fields)

```
department = "Engineering" AND data_classification = "internal" AND is_confidential = false
```

- Exact match on `department` (`filterable: true`)
- Exact match on `data_classification` (`filterable: true`)
- Scalar filter on `is_confidential` (`data_type: "boolean"`)

#### Combined Queries

```
"Show me all SOC2 compliance documents from the Product business unit"
```

- Semantic search for "SOC2 compliance" (`searchable: true` fields)
- Precise filter: `business_unit = "Product"` (`filterable: true`)

### Real-World Use Cases

#### 1. Legal Document Management

**Schema**: Practice area, client ID, confidentiality level, jurisdiction **Query**: "Find all corporate law documents for client ACME that are not confidential" **Filter**: `practice_area = "corporate_law" AND client_id = "ACME" AND confidentiality_level != "confidential"`

#### 2. Engineering Documentation

**Schema**: Product line, team, component, priority, tags **Query**: "Show me all high-priority API documentation from the backend team" **Filter**: `product_line = "API" AND team = "backend" AND priority >= 4`

#### 3. HR Document Management

**Schema**: Department, employee type, salary band, status **Query**: "Find all active full-time employees in the Engineering department" **Filter**: `department = "Engineering" AND employee_type = "full_time" AND status = "active"`

### Metadata Schema Usage

All documents in your tenant can use the metadata schema you define. This means:

1. **Consistency**: Documents can share the same tenant-level metadata structure
2. **Compliance**: Organizational policies are enforced across all documents
3. **Queryability**: You can filter and search by configured metadata attributes
4. **Scalability**: New documents can follow your established metadata model

### Migration and Updates

<Warning>
  **Schema Immutability**: Once you create a tenant with a metadata schema, the keys cannot be changed. However, you can:

 - Add new fields to `additional_metadata` (fully flexible)
 - Update values for existing `metadata` fields
  - Create new sub-tenants with different schemas if needed
</Warning>

> **Note:**
>
> - The `tenant_metadata_schema` field in the request body is optional but recommended for enterprise setups. It defines immutable tenant-level `metadata` keys that can apply to documents in this tenant.
> - For more detailed information about metadata usage, filtering, and querying, see our [Metadata documentation](/essentials/metadata).

## Sample Response

```json
{
  "status": "success",
  "message": "Tenant '99155#$@e2' created successfully.",
  "tenant_id": "B1SlKQ2j2l1aWYLa"
}
```

### Functionality

- Accepts an optional `tenant_id` via query
- Generates a new `tenant_id` if none is provided
- Creates and persists the tenant
- Returns the final `tenant_id` and a confirmation message

> Note: If your use case is to directly upload and search embeddings (without document/text ingestion), pass `"is_embeddings_tenant": true` (and optionally `"embeddings_dimension"`) in the request body to `POST /tenants/create`. This sets `sub_tenant_id = tenant_id` and prepares the tenant for embeddings-first workloads.